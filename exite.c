#include "main.h"
void exitcmd(char **argv){
        int status;

        if (argv[1]){
            status =atoi(argv[1]);
    for (i = 0; token != NULL; i++){
    free(argv[i]);
    }

    free(argv);

            exit(status);
        }else{
    for (i = 0; token != NULL; i++){
    free(argv[i]);
    }

    free(argv);

        exit(0);
        }
    }
